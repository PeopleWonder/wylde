[package]
name = "wylde-theme"
description = "Wylde visual identity — colour tokens, typography, font bundle.  Mirrors `Core/GUI/src/app.css` :root variables for the gpui-era GUI."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
gpui.workspace = true
