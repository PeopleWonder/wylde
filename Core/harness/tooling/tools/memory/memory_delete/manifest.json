{
  "id": "memory_delete",
  "name": "memory.delete",
  "description": "Delete a memory permanently (also removes any predecessors in its supersession chain). Use this tool ONLY when the user has explicitly asked to delete or forget a specific memory entry (e.g., 'forget what I said about X', 'remove that note about Y'). The post-turn extractor never deletes; only the user can. Revision via memory.update is usually preferable since it preserves history.",
  "version": "1.0",
  "service": "harness",
  "group": "memory",
  "entrypoint": "run_memory_delete",
  "requires_confirmation": true,
  "tags": ["memory", "delete"],
  "parameters": [
    {"name": "scope", "type": "string", "required": true, "description": "'long_term' | 'workspace'"},
    {"name": "id", "type": "string", "required": true, "description": "Memory id to delete."}
  ]
}
