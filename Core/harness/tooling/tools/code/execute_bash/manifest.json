{
  "id": "execute_bash",
  "name": "execute_bash",
  "description": "Execute a shell command line. Returns stdout, stderr, and return code. Timeout clamped to 300s; output streams capped at 10 KiB each. The runner does not sandbox the command — caller is responsible for vetting.",
  "version": "2.0",
  "service": "harness",
  "group": "code",
  "entrypoint": "run_execute_bash",
  "tags": ["code", "bash", "shell", "execution"],
  "parameters": [
    {"name": "command", "type": "string", "required": true, "description": "Shell command to run"},
    {"name": "timeout", "type": "number", "required": false, "default": 30, "description": "Max seconds before the process is killed (clamped to 300)"}
  ]
}
