{
  "id": "execute_python",
  "name": "execute_python",
  "description": "Execute a Python code snippet in a subprocess (`python -c`). Returns stdout, stderr, and return code. Timeout clamped to 300s; output streams capped at 10 KiB each.",
  "version": "2.0",
  "service": "harness",
  "group": "code",
  "entrypoint": "run_execute_python",
  "tags": ["code", "python", "execution"],
  "parameters": [
    {"name": "code", "type": "string", "required": true, "description": "Python source to execute"},
    {"name": "timeout", "type": "number", "required": false, "default": 30, "description": "Max seconds before the process is killed (clamped to 300)"}
  ]
}
