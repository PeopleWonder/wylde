{
  "id": "apply_patch",
  "name": "apply_patch",
  "description": "Apply a unified-diff patch to a file. Supports multi-hunk patches and verifies that deletions match the source — a mis-aligned patch errors loudly instead of corrupting the file. Use `dry_run=true` to validate without writing.",
  "version": "2.0",
  "service": "harness",
  "group": "diff",
  "entrypoint": "run_apply_patch",
  "tags": ["patch", "diff"],
  "parameters": [
    {"name": "path", "type": "string", "required": true, "description": "File to patch"},
    {"name": "patch", "type": "string", "required": true, "description": "Unified-diff text"},
    {"name": "dry_run", "type": "boolean", "required": false, "default": false, "description": "Validate without writing"}
  ]
}
