"""tools/diff/ — pure-Python diff and patch tools.

`show_diff` produces a unified diff via `difflib`; `apply_patch` applies a
multi-hunk unified-diff body and verifies deletions match the source.
"""
