"""tools/meta/ — system-introspection tools.

Tools in this group operate on Wylde itself rather than on user-domain data.
Today that's:

* :mod:`tool_search`  — natural-language search over the tool catalog.
* :mod:`graph_query`  — entity-anchored traversal over the knowledge graph.

Domain tools (web, git, code) live in sibling groups under ``tools/``.
"""
