"""tools/test/ — test runner tools.

Auto-detects pytest / vitest / jest from project marker files (pytest.ini,
pyproject.toml, package.json devDeps). Honors an explicit `framework`
parameter when given.
"""
