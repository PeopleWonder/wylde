{
  "id": "rag_misses",
  "name": "rag_misses",
  "description": "List recent queries where retrieval missed (confidence gate fired or no valid citations). Reads from the miss_log memory layer; that layer only stores miss events, so only_gated is effectively always true.",
  "version": "2.0",
  "service": "harness",
  "group": "rag",
  "entrypoint": "run_rag_misses",
  "tags": ["rag", "misses", "training", "analytics", "memory"],
  "parameters": [
    {"name": "limit", "type": "number", "required": false, "default": 100, "description": "Max rows to return (clamped 1..1000)"},
    {"name": "only_gated", "type": "boolean", "required": false, "default": true, "description": "Restrict to rows where the confidence gate fired"},
    {"name": "include_trace", "type": "boolean", "required": false, "default": false, "description": "Include the full retrieval-trace JSON (verbose)"}
  ]
}
