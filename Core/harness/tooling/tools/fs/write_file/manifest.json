{
  "id": "write_file",
  "name": "write_file",
  "description": "Write text to a file. Creates missing parent directories. Overwrites any existing content. The path is recorded on the active chat turn; the architectural wylde_check sweep runs once at end-of-turn against every file the turn wrote (no per-write block — see docs/wylde_check_rules.md).",
  "version": "2.2",
  "service": "harness",
  "group": "fs",
  "entrypoint": "run_write_file",
  "tags": ["fs", "file", "write"],
  "parameters": [
    {"name": "path", "type": "string", "required": true, "description": "Path to the file"},
    {"name": "content", "type": "string", "required": true, "description": "Text to write"}
  ]
}
