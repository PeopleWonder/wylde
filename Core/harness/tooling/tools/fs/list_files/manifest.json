{
  "id": "list_files",
  "name": "list_files",
  "description": "List the immediate contents of a directory. Returns name, type (file/dir), and size for each entry. Non-recursive.",
  "version": "2.0",
  "service": "harness",
  "group": "fs",
  "entrypoint": "run_list_files",
  "tags": ["fs", "directory", "list"],
  "parameters": [
    {"name": "path", "type": "string", "required": false, "default": ".", "description": "Directory to list"}
  ]
}
