{
  "id": "lint_rust",
  "name": "lint_rust",
  "description": "Run cargo clippy against Core/GUI/src-tauri/. Returns normalised findings. Underlying engine: cargo clippy (must be present in toolchain).",
  "version": "1.0",
  "service": "harness",
  "group": "dev",
  "entrypoint": "run_lint_rust",
  "tags": ["dev", "lint", "rust", "clippy"],
  "parameters": []
}
