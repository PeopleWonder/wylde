{
  "id": "wylde_check",
  "name": "wylde_check",
  "description": "Run Wylde's custom architectural checker (no internal HTTP, single manifest write path, dead service refs, InferenceBar purity, Gateway scope, etc.). Eight rules; returns normalised findings. Backed by Core/harness/dev/wylde_check.py.",
  "version": "1.0",
  "service": "harness",
  "group": "dev",
  "entrypoint": "run_wylde_check",
  "tags": ["dev", "lint", "architecture", "wylde-specific"],
  "parameters": [
    {"name": "only", "type": "array", "required": false, "description": "Optional list of rule names to run. Defaults to all 8."}
  ]
}
