{
  "id": "evict_model",
  "name": "evict_model",
  "description": "Immediately unload an Ollama model from VRAM (keep_alive=0). Use to free VRAM for ComfyUI etc.",
  "version": "2.0",
  "service": "harness",
  "group": "ollama",
  "entrypoint": "run_evict_model",
  "tags": ["ollama", "vram", "evict"],
  "parameters": [
    {"name": "model", "type": "string", "required": true, "description": "Model name to evict"}
  ]
}
