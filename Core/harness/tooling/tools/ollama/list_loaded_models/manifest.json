{
  "id": "list_loaded_models",
  "name": "list_loaded_models",
  "description": "List Ollama models currently loaded in memory (`GET /api/ps`). Returns name, size, VRAM usage, and expiry time per model.",
  "version": "2.0",
  "service": "harness",
  "group": "ollama",
  "entrypoint": "run_list_loaded_models",
  "tags": ["ollama", "vram", "list"],
  "parameters": []
}
