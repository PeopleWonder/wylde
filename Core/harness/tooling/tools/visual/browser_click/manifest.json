{
  "id": "browser_click",
  "name": "browser_click",
  "description": "Click a browser element by CSS selector or by viewport coordinates via Playwright.",
  "version": "2.0",
  "service": "harness",
  "group": "visual",
  "entrypoint": "run_browser_click",
  "tags": ["visual", "browser", "click", "playwright"],
  "parameters": [
    {"name": "selector", "type": "string", "required": false, "description": "CSS selector to click. Mutually exclusive with x/y."},
    {"name": "x", "type": "number", "required": false, "description": "X coordinate within the viewport"},
    {"name": "y", "type": "number", "required": false, "description": "Y coordinate within the viewport"},
    {"name": "button", "type": "string", "required": false, "default": "left", "description": "'left', 'right', or 'middle'"},
    {"name": "timeout", "type": "number", "required": false, "default": 10000, "description": "Click timeout in milliseconds (selector path only)"}
  ]
}
