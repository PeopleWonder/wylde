{
  "name": "extension_bridge",
  "description": "External named-pipe surface for the in-process extension bridge. Lets callers with no in-process Python (the Rust Gateway port) reach the extension dispatcher. The bridge proper (loader / registry / dispatcher) stays in-process; this service is a thin pipe wrapper around extension_bridge.dispatch_external. Pipe: \\\\.\\pipe\\wylde-extension-bridge.",
  "version": "1.0.0",
  "enabled": true,
  "entry_point": "py -3 -m Extensions.extension_bridge.run",
  "depends_on": [],
  "tier": "core",
  "pipe": "wylde-extension-bridge",
  "actions": [
    "extensions.dispatch"
  ]
}
