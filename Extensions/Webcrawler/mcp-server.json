{
  "name": "Webcrawler",
  "description": "Fetch raw URL contents, scrape HTML with CSS selectors, extract structured data via rule sets. Migrated to MCP via wylde_mcp_py_shim — the legacy handler.py is untouched.",
  "version": "1.0",
  "enabled": false,
  "transport": "stdio",
  "command": [
    "${WYLDE_PYTHON}",
    "-m",
    "Extensions._shim.server",
    "--extension",
    "Webcrawler"
  ],
  "cwd": "${WYLDE_ROOT}",
  "env": {
    "PYTHONPATH": "${WYLDE_ROOT}"
  },
  "capabilities": ["egress.web"],
  "health": {
    "method": "ping",
    "interval_s": 30,
    "timeout_s": 5
  }
}
