{
  "id": "scrape",
  "name": "scrape",
  "description": "Scrape HTML content from a URL with optional CSS selectors. Returns the raw page plus selector-extracted text values. External egress routes through Wylde.Gateway.client.",
  "version": "1.0",
  "service": "extension",
  "group": "web",
  "extension": "Webcrawler",
  "entrypoint": "run_scrape",
  "tags": ["web", "scraping", "html"],
  "parameters": [
    {"name": "url", "type": "string", "required": true, "description": "URL to scrape"},
    {"name": "selectors", "type": "array", "required": false, "description": "CSS selectors to extract; each matched element's stripped text is returned"},
    {"name": "timeout", "type": "number", "required": false, "default": 10, "description": "Request timeout in seconds"}
  ]
}
