{
  "id": "extract",
  "name": "extract",
  "description": "Extract structured data from HTML using a rule set ({field: {selector, attribute, multiple}}). Accepts either a 'url' to fetch first or raw 'html'.",
  "version": "1.0",
  "service": "extension",
  "group": "web",
  "extension": "Webcrawler",
  "entrypoint": "run_extract",
  "tags": ["web", "extraction", "parsing"],
  "parameters": [
    {"name": "url", "type": "string", "required": false, "description": "URL to fetch and extract from (alternative to html)"},
    {"name": "html", "type": "string", "required": false, "description": "Raw HTML content (alternative to url)"},
    {"name": "extraction_rules", "type": "object", "required": true, "description": "Rules: {field: {selector, attribute, multiple}}"}
  ]
}
