# Wylde VPN

WyldeLink — peer-to-peer VPN with WireGuard tunnels and TURN relay for remote access.

- Transport: `\\.\pipe\wylde-vpn` (HTTP port 8020)
- Run: `python run.py`
- Install: `pip install -r requirements.txt`
