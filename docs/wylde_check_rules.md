# wylde_check rules

`wylde_check` is Wylde's architectural linter. It runs once at end-of-task from both agent runtimes that drive Wylde development (Claude Code and the local Wylde harness) and is also callable directly via `wylde_check.run_all()` (full sweep) or `wylde_check.check_one_file(rel_path, content)` (per-file fast path). Findings are tagged ERROR, WARNING, or INFO.

### Invocation model

Both runtimes used to fire the architectural check **per write**:

- **Claude Code** ran `lint_hook.py` from the PostToolUse hook in `.claude/settings.json` after every Edit/Write/MultiEdit — a full 19-rule sweep against the tree each time.
- **Wylde harness** (`Core/harness/turn.py`) ran `prewrite.evaluate_prewrite` from inside `write_file.py` / `edit_file.py` before each write touched disk. ERROR findings blocked the write entirely (status `"blocked"`); the LLM had to recover mid-stream.

With 30 rules and Colossus-1 backend latency, the per-write cadence compounded badly on multi-edit tasks — a 200-edit task paid the full linter cost 200 times when only the final state mattered.

Both runtimes now fire the check **once per end-of-task**:

| Runtime | Trigger | What runs | What happens to findings |
| --- | --- | --- | --- |
| Claude Code | `Stop` event from `.claude/settings.json` → `lint_hook.py` | `wylde_check.run_all()` over the active tree | ERROR → exit 2 (Claude is forced to keep working until findings clear or the user intervenes); WARN-only or clean → exit 0 |
| Wylde harness | End of every chat turn in `_drive_turn_inner`, just before `turn_complete` fires | `wylde_check.check_one_file` over `state.files_written` (paths recorded by `write_file` / `edit_file` during the turn) | ERROR findings emitted as a single `tool_warning` event on `state.tool_events` with `source: "wylde_check_end_of_turn"`; per-file WARNINGs dropped (cross-file warnings still surface via `run_all()` at end-of-task) |

Same rules, same coverage, two different end-of-task triggers. The trade-off is the same on both sides: faster tasks, no per-write block, the agent learns about violations after the response is sealed rather than mid-stream. The old per-write block existed to stop the LLM from doubling down on a broken pattern across multiple writes; the equivalent guarantee at end-of-task comes from the agent re-reading its own warnings on the next turn.

The harness side targets only the files the turn touched (via the cheap `check_one_file` path) rather than re-sweeping the whole tree per turn — that would defeat the latency win. The full `run_all()` sweep belongs in end-of-task verification (Claude Code's `Stop` hook, the manual `python -c ...` invocation, or CI).

`force=True` no longer exists on `write_file` / `edit_file` — there's no per-write block to bypass.

### Severity & enforcement (Claude Code `Stop` mode)

- **ERROR** findings cause the hook to exit 2. On a `Stop` event, exit 2 prevents Claude from ending the turn and surfaces stderr back into the conversation — Claude is forced to address findings before the turn closes (or the user manually intervenes).
- **WARNING** findings print to stderr but the hook exits 0, so the turn ends normally.
- **No findings** → silent exit 0.

The `Stop` event does not support a `matcher` field, so the hook fires unconditionally on every turn boundary.

Manual invocation is still available: `python -c "from Core.harness.dev.wylde_check import run_all; print(run_all())"` or via the harness tool that wraps it. The `lint_hook.py` per-file mode is still callable for ad-hoc checks of a single file (`file_path` via stdin JSON, `$CLAUDE_FILE_PATH`, or argv[1]).

## Rules

1. **no_internal_http** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had required services to use named pipes rather than HTTP for internal IPC (Gateway, the Ollama client, and the Memgraph Bolt driver were the documented exemptions). Removed from the dispatcher, module, and tests. No Rust counterpart exists yet — the internal-port constants are retained in `_config.py` for a queued `no_internal_http_rust`.
2. **manifest_paths** — _RETIRED (2026-07-20 dead-rule retirement): `Core/Lifecycle/daemon_state.py` and the per-service `run.py` files it checked were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had stopped a service with a daemon-managed `_start_X` from also calling `write_manifest()` from its own `run.py`. Removed from the dispatcher, module, and tests.
3. **tool_id_regex** — _RETIRED (2026-07-20 dead-rule retirement): the `**/tools/**/manifest.json` targets were deleted in the Rust cutover (the `tools/` tree is gone), so the rule inspected nothing and passed vacuously._ It had checked every tool manifest `id` and `name` against `/^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)*$/`. Removed from the dispatcher, module, and tests.
4. **action_registry** — _RETIRED (2026-07-20 dead-rule retirement): the six pipe modules (like `Gateway/pipe.py`) it read were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had enforced that pipe-action names registered inside a single pipe module are unique. Removed from the dispatcher, module, and tests.
5. **import_paths** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had required active code to use bare `Core.*` imports, reserving the non-canonical `Wylde.Core.*` form for tests that need the try-fallback. Removed from the dispatcher, module, and tests.
6. **dead_service_refs** — known-dead service names (renamed or removed during the Phase 9–12 refactor — former orchestrator/RAG pipes, the pre-merge security/web services, the dissolved voice assistant module, etc.) don't appear in active code. The canonical list lives in `DEAD_SERVICE_NAMES` inside `wylde_check.py`. An inline `# wylde-check: dead-ref-ok` marker suppresses a single line.
7. **inferencebar_purity** — _RETIRED (slice-11 cutover, 2026-05-29)._ Keyed on `InferenceBar.svelte`; the Svelte tree (`Core/GUI/src/`) was deleted at the cutover, leaving the rule no possible target. Removed from the dispatcher, module, and tests.
8. **gateway_scope** — _RETIRED (2026-07-20 dead-rule retirement): `Gateway/routes/` was deleted in the Rust cutover (the Rust `wylde-gateway` crate is the live server), so the rule inspected nothing and passed vacuously._ It had required every FastAPI route under `Gateway/routes/` to start with a documented prefix. Removed from the dispatcher, module, and tests.
9. **gui_action_contract** — _RETIRED (slice-11 cutover, 2026-05-29)._ Keyed on Svelte `pipeAction(SVC_X, …)` callsites under the deleted `Core/GUI/src/`. The gpui successor is rule 38 (`panel_verbs_exist_in_harness_registry`), which validates panel `pipe::call` / `stream_call` verbs against the live service registries. Removed from the dispatcher, module, and tests.
10. **gui_no_backend_bypass** — the gpui GUI panels + shell don't reference backend-owned storage paths (memory indexes, `data/` directories) or service `manifest.json` paths as literal strings — everything goes through a `wylde_gui_pipe` action. Repointed at the slice-11 cutover from the deleted Svelte `src/` + Tauri `src-tauri/src/` trees to the gpui Rust source under `Core/GUI/Frontend` + `Core/GUI/Shell`. The panel-registry aggregator under `Core/GUI/Manifest/` is out of scope — reading panel `manifest.json` files is its whole job.
11. **gui_pipe_constants** — _RETIRED (slice-11 cutover, 2026-05-29)._ Keyed on `SVC_X = "wylde-..."` JS constants in the deleted `Core/GUI/src/lib/api.js`. Subsumed by the gpui contract rules (38 `panel_verbs_exist_in_harness_registry`, 41 `rest_routes_exist_in_service`), which validate panel→service wiring directly. Removed from the dispatcher, module, and tests.
12. **tool_docstring_required** — _RETIRED (2026-07-20 dead-rule retirement): the `**/tools/**/manifest.json` targets were deleted in the Rust cutover (the `tools/` tree is gone), so the rule inspected nothing and passed vacuously._ It had required every tool manifest to carry a non-empty `description` of ≥20 chars as the tool's LLM-facing contract. Removed from the dispatcher, module, and tests.
13. **logging_setup_only** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had confined root-logging configuration to `Core/shared/logging_setup.configure_logging()`; its Rust twin `logging_setup_only_rust` (label 28) covers the live tracing-subscriber surface. Removed from the dispatcher, module, and tests.
14. **no_external_subprocess** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had restricted `subprocess.Popen` / `.run` / `.call` / `os.spawn*` to the Lifecycle daemon and a documented allowlist; its Rust twin `no_external_process_spawn_rust` (label 29) covers `Command::new` in the live crates. Removed from the dispatcher, module, and tests.
15. **spawn_paths_exist** — _RETIRED (2026-07-20 dead-rule retirement): `Core/Lifecycle/daemon_state.py` was deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had verified that every `python -m <module>` or script-path argument constructed inside `daemon_state.py` resolved to an importable module or existing file, catching typos and renames that didn't propagate to the daemon. Removed from the dispatcher, module, and tests.
16. **run_py_entry_point** — _RETIRED (2026-07-20 dead-rule retirement): the per-service `run.py` entry points were deleted in the Rust cutover (no `run.py` exists anywhere now), so the rule inspected nothing and passed vacuously._ It had required every top-level service folder to use exactly `run.py` as its entry point, rejecting `<svc>_run.py` / `start_<x>.py` / `launcher*.py` variants. Removed from the dispatcher, module, and tests.
17. **pipe_name_convention** — every Windows named-pipe `wylde-<name>` literal in active code matches `^wylde-[a-z][a-z0-9-]*$` (lowercase, dash-separated). Underscore typos like `\\.\pipe\wylde_voice` are also flagged when they appear in a `pipe\…` path.
18. **run_py_startup_sequence** — _RETIRED (2026-07-20 dead-rule retirement): the per-service `run.py` files and `data/manifests/` it read were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had checked that each service self-attested the four-phase startup convention (`configure_logging` → `write_manifest` → `start_heartbeat` → `serve_loop`). Removed from the dispatcher, module, and tests.
19. **shutdown_handler_marks_stopped** — _RETIRED (2026-07-20 dead-rule retirement): the per-service `run.py` files and `data/manifests/` it read were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had required services to flip `shutdown_attested: true` in the manifest when `manifest.mark_stopped` fires. Removed from the dispatcher, module, and tests.
20. **file_size_limit** — flat 700-line cap on every active Rust source (`rust/crates/*/src/**/*.rs` and `Core/GUI/**/*.rs`, excluding `/target/`) — repointed from Python to Rust on 2026-07-20. No production/test split; the same rule applies everywhere. When a file blows the cap, the right move is almost always to split it along its natural seams (one module per submodule, one route group per file, etc.). Counts raw lines including blanks and comments — file *length* is what hurts editing and review, not LOC density. A documented `_FILE_SIZE_QUEUED_SPLITS` allowlist inside the rule now holds the 91 Rust files that were over-cap at the repoint (worst: `Core/GUI/Frontend/Panels/Chat/src/chat_panel.rs` at 5298 lines); each entry has a one-line comment naming the planned split shape, and the rule re-engages on a file once its entry is removed as the split lands.
21. **test_init_present** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had required every `tests/` folder under an active root to contain an `__init__.py` so pytest's rootdir discovery couldn't load the wrong `conftest`. Removed from the dispatcher, module, and tests.
22. **memory_layer_boundaries** — _RETIRED (2026-07-20 dead-rule retirement): the owning `Core/harness/memory/` tree was deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had confined literal memory-layer storage paths (`memory/indexes/`, `memory/workspace_memories/`, `memory/long_term/`, `memory/short_term/`) to code inside `Core/harness/memory/`. Removed from the dispatcher, module, and tests.
23. **action_docstring_required** — _RETIRED (2026-07-20 dead-rule retirement): the six pipe modules it walked were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had required every registered pipe-action handler to carry a non-empty docstring (≥15 chars), since handlers are the contract surface the LLM sees. Removed from the dispatcher, module, and tests.
24. **no_bare_except** — _RETIRED (2026-07-20): Python-only rule; the Rust cutover deleted all production Python, so its only remaining inputs were the wylde_check tooling itself._ It had flagged bare `except:` always, and multi-statement `except Exception:` that neither logged nor re-raised nor referenced the bound exception. Removed from the dispatcher, module, and tests.
25. **service_owns_its_state** — a service only reads/writes paths inside its own data directory; cross-service state access goes through the peer's pipe action, not the filesystem. `Core/Lifecycle/` (the daemon) and `Core/shared/` are unrestricted; the GUI surface is covered by rule 10 instead of double-flagging here. A small documented exemption list in the rule body covers files that legitimately straddle two services as a hand-off placeholder (the entry must reference a punch-list item in its module docstring so the rewire is tracked).
26. **import_paths_rust** — Rust crates may only depend on each other via `wylde-shared`. Any `use wylde_<other_crate>::` outside the crate's own name (with `wylde_shared` always allowed) is flagged. Deep `super::super::` traversal is also flagged as a sign the module graph is wrong — by the time you need to walk three module levels up, the file belongs somewhere else. Walks `rust/crates/*/src/**/*.rs`; integration tests under `rust/crates/*/tests/` are exempt.
27. **no_silent_error_swallow_rust** — `let _ = <result>;` and trailing `.ok();` patterns that drop a Result without logging are flagged. The `let _` heuristic uses an allowlist of known-Result-returning expressions (`atomic_write`, `fs::*`, `.send(`, `.spawn(`, etc.) and a small known-not-Result allowlist (`tokio::spawn`, `Vec::new`, `Arc::clone`, …) to keep false positives down. An inline `// wylde-check: discard-result-ok` marker suppresses a deliberate discard (typical for shutdown paths where logging the error would itself fail).
28. **logging_setup_only_rust** — only `wylde_shared::logging::configure_logging` may build or initialise the tracing subscriber. Direct `tracing_subscriber::fmt()`, `tracing_subscriber::registry()`, and `tracing::subscriber::set_global_default` calls are flagged everywhere except `wylde-shared/src/logging.rs` (the source of truth). Mirrors rule 13 on the Python side.
29. **no_external_process_spawn_rust** — `std::process::Command::new`, `tokio::process::Command::new`, and bare `Command::new` are restricted to the `wylde-lifecycle` crate. Supervising other processes is the lifecycle daemon's job, not a per-service responsibility. Mirrors rule 14 on the Python side.
30. **gui_error_reporting** — _RETIRED (slice-11 cutover, 2026-05-29)._ Keyed on Svelte `console.error` / `toast.error` / `alert` sinks + swallowed `catch` blocks under the deleted `Core/GUI/src/`. The gpui panels surface errors as `Result`-typed panel state rendered inline, a structurally different model with no `console.error` analogue — a gpui-era error-reporting rule is a possible post-alpha addition. Removed from the dispatcher, module, and tests.
31. **shutdown_reaps_manifest_orphans** — the lifecycle daemon must reap manifest orphans before it starts any service: `rust/crates/wylde-lifecycle/src/state/orphan_sweep.rs` must declare a `pub fn` matching `*orphan*sweep*`, and `rust/crates/wylde-lifecycle/src/daemon.rs` must **call** it on the boot path ahead of the first `start_<service>()`. Why: a manifest left by an ungraceful prior exit (Ctrl-C / `taskkill` / SIGKILL) still marks its service alive with a now-dead pid; the recurring 60s sweep only fires *after* the boot spawns, so without a one-shot sweep first the stale manifest survives a restart and the service stays dark — the harness / extension_bridge / ollama outage of 2026-05-31. **Repointed for #116.** The rule previously targeted `stop_all_daemon_managed` in `Core/Lifecycle/daemon_state/__init__.py`, looking for a `reap*orphan*` call at *teardown*. That whole Python tree was deleted in the Rust cutover, so the rule was red-but-unnoticed (nothing ran it — see #114). The guarantee did not disappear, it **moved**: teardown no longer reaps (Rust `stop_all_daemon_managed` only calls `stop_orphan_sweep()`, which *halts* the recurring sweep so an in-flight tick can't rewrite a manifest mid-teardown), and boot now sweeps. The rule follows the guarantee to its new home. Comment lines are stripped before matching, `(stop|halt|cancel|abort)_*orphan*sweep*` is rejected (halting a sweep is not performing one), and a missing target file **fires** rather than passing.
32. **manifest_sandbox_required** — _RETIRED (2026-07-20 dead-rule retirement): `Core/Lifecycle/tests/` and `Core/harness/tests/` were deleted in the Rust cutover, so the rule inspected nothing and passed vacuously._ It had required manifest-touching tests to rebind `_MANIFEST_DIR` to a tmp path so an unsandboxed test couldn't trip the orphan reaper into killing real wylde services. Removed from the dispatcher, module, and tests.
33. **no_cross_panel_imports** — every `Core/GUI/Frontend/Panels/*/Cargo.toml` may only depend on the shared-infrastructure crates (`wylde-theme`, `wylde-gui-pipe`, `wylde-gpui-input`, `wylde-panel-registry`). Any other `wylde-panel-*` dep is a panel-to-panel import that would couple two first-party panels at compile time; any other `wylde-*` crate is the panel reaching for backend internals instead of going through `wylde-gui-pipe`. Cross-panel state belongs on `wylde-gui-pipe::nav_bus` or in `wylde-panel-registry`. **Section-aware since #236:** a backend crate named in `PANEL_DEV_ONLY_BACKEND_EDGE_EXEMPTIONS` is permitted in `[dev-dependencies]` **only** — `resolver = "2"` keeps a dev-dependency out of the shipped Shell, so test code may drive the real backend while the production boundary stays absolute. The same dep in `[dependencies]` is still an error, and the carve-out is per-`(panel, crate)` edge: it does not generalise to other panels, and it never permits a sibling *panel* dep (that needs its own `PANEL_CROSS_PANEL_EDGE_EXEMPTIONS` entry).
34. **no_legacy_gui_imports_in_panels** — no `tauri::*` use paths anywhere under `Core/GUI/Frontend/Panels/**`. Panel crates are gpui-native; the legacy Tauri+Svelte tree at `Core/GUI/src/` + `Core/GUI/src-tauri/` was deleted at the slice-11 cutover (2026-05-29). The Svelte matcher was removed on 2026-07-20 — zero `.svelte`/`.js`/`.ts` files remain, and its only finding had been a false positive on a file-icon table row. The rule remains relevant — it stops a panel from reintroducing a `tauri::` dependency — and strips comments first so historical references in module docs don't false-fire.
35. **webview_only_in_extension_handlers** — `wry::*` imports are reserved for `Core/GUI/Frontend/Extension_handlers/WebView/**`. The Shell legitimately renders iframe slots, but only through `wylde_webview::IframeHost` / `wylde_webview::probe_url` — never by reaching for `wry` directly. The rule guards against a first-party panel embedding a browser engine instead of rendering native gpui.
36. **first_party_manifest_must_be_gpui_view** — first-party panel manifests under `Core/GUI/Frontend/Panels/**` declare `source.kind: "gpui_view"` for every entry in their `panels` array; the `iframe` shape is reserved for extensions. The symmetric extension-manifest half — which required `source.kind: "iframe"` for every `ui_panels` entry in manifests under `Extensions/<X>/` — was removed on 2026-07-20 because `Extensions/` no longer exists, leaving only the first-party check.
37. **panel_crate_must_be_workspace_member** — every `Core/GUI/Frontend/Panels/*/Cargo.toml` that exists on disk appears in the `members = [...]` array of `Core/GUI/Cargo.toml`, AND every `Frontend/Panels/*` entry in `members` resolves to a real `Cargo.toml` on disk. Either-direction failure either skips the crate at build time or makes `cargo metadata` refuse the workspace; the rule catches both at the architecture layer so the failure surfaces at lint time, not at the first compile.
38. **panel_verbs_exist_in_harness_registry** — for every `wylde_gui_pipe::call(SVC, "POST", "/__action__", json!({"action": "X", ...}))` and every `wylde_gui_pipe::stream_call(SVC, "X", ...)` in `Core/GUI/Frontend/Panels/*/src/ipc.rs` whose first arg resolves to a registry-indexed service, the named verb `X` must appear in that service's registry. Indexed services today: `wylde-harness` (Rust `ALL_PIPE_ACTIONS`), `wylde-extension-bridge` / `wylde-ollama` / `wylde-voice` (each crate's `const ALL_ACTIONS: [&str; N] = [...]`). **Registry repointed for #116:** the harness registry is now the Rust `ALL_PIPE_ACTIONS` array alone, at `rust/crates/wylde-harness/src/pipe/mod.rs`. The two constants this rule used to read — `rust/crates/wylde-harness/src/pipe.rs` (renamed when the crate grew a module directory) and `Core/harness/pipe/__init__.py` (the Python strangler-fig half, deleted outright in the Rust cutover) — were **both absent from `develop`**, so the registry loaded empty and the rule passed vacuously. An unloadable or empty registry is now a hard `error`, never a silent pass: a rule that cannot load its input has not passed, it has failed to run. A service *declared* in `RUST_SERVICE_REGISTRIES` whose registry loads empty is likewise an `error` — declared-but-unloadable means the crate was restructured out from under the rule. Services with no declared registry at all (`wylde-vpn`, the Gateway's REST surface) are still legitimately skipped. Catches typo'd verbs and as-yet-unimplemented surfaces at edit time instead of as a runtime `no_action` error. Calls to services without a discoverable registry (`wylde-vpn`, `wylde-gateway`'s REST surface) are intentionally skipped — the Gateway HTTP surface lives in rule 41 instead. Dynamic-action call sites (where the action key is a parameter) are skipped too. Fix is to add the verb to the service, fix the typo, or remove the call.
39. **nav_targets_exist** — every literal-string `request_nav("X")` / `nav_bus::request_nav("X")` / `wylde_gui_pipe::request_nav("X")` call under `Core/GUI/**/*.rs` (excluding the legacy Tauri+Svelte tree and the `nav_bus.rs` source itself) must resolve to a panel actually declared by some `manifest.json` under `Core/GUI/Frontend/Panels/**`. The runtime nav bus silently absorbs unknown keys (`request_nav` returns `false`), so an unknown target shows up as a no-op the user can't debug; this rule forces the panel manifests and the nav callsites to stay in sync. Constant-propagation: `request_nav(IDENT)` where the file declares `[pub] const IDENT: &str = "core/foo"` resolves through the file-local constant map and is checked the same as a literal. Variable-argument call sites whose value isn't a const string are intentionally skipped — runtime-built keys aren't statically checkable.
40. **required_services_includes_called_services** — under-declaration (ERROR) and over-declaration (WARNING). Under-declared: a service called from a panel's `src/ipc.rs` that isn't in `required_services`; the Shell's ServiceUnavailable stub won't fire when that service is down, leaving the panel rendering a broken half-state. Over-declared: a service listed in `required_services` that the panel doesn't actually call; the panel grays out unnecessarily when that service is down even though it would still work. Calls whose service arg can't be statically resolved (parameter-passed) are skipped; both literal `"wylde-foo"` and `pub const SVC_X: &str = "..."` indirection are supported. Panels whose docstring-documented design is "render degraded cards instead of the ServiceUnavailable stub" can opt out by adding `"wylde_check_opt_outs": ["required_services_includes_called_services"]` at the manifest's top level — used today by the Dashboard panel (every card soft-fails independently).
41. **rest_routes_exist_in_service** — _RETIRED (2026-07-20 dead-rule retirement): no panel declares an `SVC_GATEWAY` constant, so there are zero checkable callsites — the Gateway REST route registry loads but nothing targets it, so the rule inspected nothing and passed vacuously._ It had matched every literal-shape `wylde_gui_pipe::call(SVC, "METHOD", "/api/...", ...)` from a panel against a route in the destination service's axum routing table. Removed from the dispatcher, module, and tests.
42. **manifest_factory_resolves** — every first-party panel `manifest.json`'s `source.factory` string must resolve to a real `pub fn` in a real workspace-member crate. Format: `<crate_underscores>::<Type>::<fn>` or `<crate_underscores>::<fn>`. The crate name is matched against the canonical `name` field of every workspace member's `Cargo.toml` (after `-` → `_` normalisation); the function name is grepped as `pub fn <name>(` (or `pub async fn`, `pub(crate) fn`, etc.) anywhere under the crate's `src/`. Intermediate `Type::` segments are accepted but not verified — they're enforced at build time when the aggregator calls into the factory. Catches deleted/renamed factory entry points at edit time so the aggregator doesn't blow up at build with an opaque link error.
43. **stream_call_must_handle_cancel** — every `wylde_gui_pipe::stream_call(...)` invocation under `Core/GUI/Frontend/Panels/**` must retain the returned `PipeStream`. Safe shapes: `let stream = stream_call(...)`, `self.stream = Some(stream_call(...))`, `stream_call(...)?`, `stream_call(...).await?`, `return stream_call(...);`, and trailing-expression position (the call is the block's value, no terminating `;`). Flagged shapes: `let _ = stream_call(...)` (explicit discard) and bare `stream_call(...);` expression-statement (anonymous discard). When the discard is genuinely intentional (e.g. a fire-and-forget probe at shutdown), annotate the call site with `// wylde-check: stream-discard-ok` on the same line or the line directly above. Heuristic limitation: helpers that wrap `stream_call` and return the result without binding are accepted (trailing-expression detection); helpers that accept a `PipeStream` parameter and return it without naming the original `stream_call` site are out of scope.
44. **launcher_enumerates_services_from_manifests** — the launcher (`Core/Lifecycle/launcher.py`) builds its service set from the filesystem-as-registry (`Network/services.yaml` + per-service `manifest.json`), never a hardcoded roster. Two-pronged on the Python launcher: it must reference a manifest/registry loader (`load_services` / `load_manifest` / `list_service_folders`) AND must not assign a module-level UPPERCASE `SERVICES` list literal (case-sensitive, so the everyday `services = load_services()` local never matches). The Rust lifecycle crate (`rust/crates/wylde-lifecycle/`) is held to the negative half only — its tier=core `start_<name>` sequence is bespoke, documented bring-up by design (Memgraph forks a JVM, the harness can run in-process, several services flip Python⇄Rust on a per-service env var, and the order encodes hard runtime deps), but a hardcoded `const SERVICES: [&str; N]` roster *is* flagged. Added at the slice-11 cutover; enforces the Wylde user's modularity directive that launcher rules cover all services via the manifests.
45. **shutdown_enumerates_services_from_manifests** — the canonical drain the GUI reaches via `lifecycle.shutdown_all` — today `stop_all_daemon_managed` in `rust/crates/wylde-lifecycle/src/state/mod.rs`, derived from the `DAEMON_MANAGED` table in `rust/crates/wylde-lifecycle/src/daemon_managed.rs` (repointed for #101; the `Core/Lifecycle/shutdown.py` this line used to name was deleted in the Rust cutover — stale prose corrected in #116) orders teardown from the running set + manifest `shutdown_order` (reverse-launch / reverse-topo by default, per-service override), not a hardcoded list: it must reference the running-set / manifest enumeration (`get_running` / `_shutdown_sequence` / `load_manifest` / `shutdown_order`) and carry no `SERVICES` literal. The gpui-side `Core/GUI/Shell/src/shutdown.rs` must *delegate* to that drain by dispatching `lifecycle.shutdown_all`. **Exemption withdrawn (issue #124, 2026-07):** this rule used to add that the GUI's `WYLDE_SERVICE_PROCESSES` / `WYLDE_KILL_TARGETS` constants were the recognised hard-kill image-name fallback and were "deliberately NOT treated as a roster" — i.e. exempt from any coverage check. That exemption was load-bearing for a real bug: both constants were hand-typed arrays naming four of the eleven killable services, and the drain wait polled the *same four*, so it concluded "drained" as soon as those exited and the hard-kill path never ran. Quit reported success with eight services still holding VRAM and named pipes. Those constants no longer exist; both sets now derive from `wylde_stack::shutdown_targets`, and the exemption is withdrawn. Rule 45 still checks only the `shutdown_sequence()` derivation and the `lifecycle.shutdown_all` delegate token — it does **not** count service coverage, and a rule 45 pass says nothing about it. The counting gate is a Rust test, `rust/crates/wylde-stack/tests/shutdown_target_coverage.rs`, which lives in the `rust/` workspace because that is the only workspace whose `cargo test` runs in CI (`Core/GUI` is build-only — see #95); it also reaches across the workspace boundary to fail if `shutdown.rs` reintroduces a hand-typed image list. Added at the slice-11 cutover; exemption withdrawn for #124.
