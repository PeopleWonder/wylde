//! Scoped-lens region derivation (concept-routing plan §6.2).
//!
//! **Stub — filled in by R3.** Will compute the region string (active file →
//! subsystem) that narrows each activated concept's members via the *existing*
//! `concepts/lens.rs::lens` in `wylde-workspaces` — no new lens code, only the
//! region computation lives here. Inert until injection (R2) gives the lens
//! something to narrow.
