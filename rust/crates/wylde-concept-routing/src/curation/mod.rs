//! Curate-before-inject (concept-routing plan §4, requirement 2).
//!
//! **Stub — filled in by R2.** Will hold `candidate` (assemble the menu
//! payload from a [`crate::router::CandidateSet`] + resolved anchors) and
//! `apply` (diff the user's add/remove choices into the final inject set). The
//! `CandidateSet` the R1 router already produces is the menu's data source, so
//! nothing here is needed until injection exists.
