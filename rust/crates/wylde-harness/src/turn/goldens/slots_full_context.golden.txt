

### User profile
Name: Golden User
Style: terse
User rules (follow verbatim):
Always show diffs.

### Conversation memory
- the user is renaming the gather module
- tests must stay green

### Conversation summary
The user has been refactoring the context gather flow.

### Long-term memory
- prefers Rust over Python for new modules
- works from a Windows box

### Workspace insights
- the gather refactor must keep render byte-stable
- goldens bless via WYLDE_BLESS_GOLDENS

### Vocabulary
- {{the_gather}} — the pre-LLM context gather (code symbol `gather_with`)

### Concepts
Context gather — assembles the layered turn context (depends on token budget; not related to streaming)

`src/turn/context_gather.rs` (lines 1-9)
pub(crate) async fn gather_with() {}

### Workspace context
## Persona
Be precise.

## Workspace memory
- uses cargo nextest

## Workspace files

fn gather_with() { /* fixture snippet */ }

### Code graph context
Symbol `gather_with` — src/turn/context_gather.rs:404
pub(crate) async fn gather_with(...) {}
  called by `handle_run_turn` (src/turn/actions.rs)
  calls `evict` (src/turn/token_budget.rs)